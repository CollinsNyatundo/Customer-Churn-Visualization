# DO NOT commit real secrets. Use sealed-secrets or external-secrets operator.
# Template only — replace base64 values before applying.
apiVersion: v1
kind: Secret
metadata:
  name: churn-secrets
  namespace: churn-ml
type: Opaque
data:
  API_KEYS: ""                  # base64 encoded comma-separated keys
  SLACK_WEBHOOK_URL: ""         # base64 encoded webhook URL
  SMTP_PASSWORD: ""             # base64 encoded SMTP password
